name: Limpeza da Vercel concluída

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  status:
    runs-on: ubuntu-latest
    steps:
      - name: Confirmar conclusão
        run: echo "Os projetos duplicados da JuliLar já foram removidos. Mantidos somente julilar-deploy-novo e volumi-moveis."
